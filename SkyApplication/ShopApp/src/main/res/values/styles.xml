<resources>

    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/main_colorPrimary</item>
        <item name="colorPrimaryDark">@color/main_colorPrimaryDark</item>
        <item name="colorAccent">@color/main_colorAccent</item>
        <item name="elevation">0dp</item>
        <item name="android:windowIsTranslucent">true</item>

        <!-- activity窗口切换动画 -->
        <!--<item name="@android:windowAnimationStyle">@style/activity_change_anim_style</item>-->
    </style>

    <!-- 沉浸式 -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- 非沉浸式 可以自定义状态栏颜色 -->
    <style name="ColorTranslucentTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- toolbar默认样式 -->
    <style name="Theme.customer.ToolBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="actionMenuTextColor">@color/main_colorPrimary</item>
    </style>

    <!-- toobart弹出层的样式 -->
    <style name="OverflowMenuStyle" parent="@style/Widget.AppCompat.PopupMenu.Overflow">
        <!-- 是否覆盖锚点，默认为true，即盖住Toolbar -->
        <item name="overlapAnchor">false</item>
        <!-- 弹出层背景颜色 -->
        <item name="android:popupBackground">@color/main_colorPrimary</item>
        <!-- 弹出层垂直方向上的偏移，即在竖直方向上距离Toolbar的距离，值为负则会盖住Toolbar -->
        <item name="android:dropDownVerticalOffset">5dp</item>
        <!-- 弹出层水平方向上的偏移，即距离屏幕左边的距离，负值会导致右边出现空隙 -->
        <item name="android:dropDownHorizontalOffset">0dp</item>
    </style>

    <!-- 首页TAB样式 -->
    <style name="tab_navigation_style">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">@dimen/sky_dp_size_56</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- 侧边栏[文字] -->
    <style name="normal_sider_font_style">
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/sky_font_size_15</item>
        <item name="android:textColor">@color/sky_color_4d4d4d</item>
    </style>
    <!-- 可按压的控件 -->
    <style name="normal_viewgroup_selector">
        <item name="android:background">@drawable/app_normal_grey_selector</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
    </style>

    <!-- 自定义dialog -->
    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@color/sky_color_transparent_white</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!-- 账户中心头部文字 -->
    <style name="app_account_top_font">
        <item name="android:textSize">@dimen/sky_font_size_13</item>
        <item name="android:textColor">@color/sky_color_333333</item>
    </style>
    <style name="app_account_bottom_font">
        <item name="android:textSize">@dimen/sky_font_size_13</item>
        <item name="android:textColor">@color/sky_color_cd7e77</item>
    </style>

    <!-- 提现账户文字 -->
    <style name="app_apply_account_left_font">
        <item name="android:textSize">@dimen/sky_font_size_16</item>
        <item name="android:textColor">@color/sky_color_333333</item>
    </style>
    <style name="app_apply_account_right_font">
        <item name="android:textSize">@dimen/sky_font_size_13</item>
        <item name="android:textColor">@color/sky_color_989898</item>
    </style>
    <!-- 账户中心 -->
    <style name="CustomAccountTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/sky_color_00a5ea</item>
        <item name="tabIndicatorHeight">@dimen/sky_dp_size_2</item>
        <item name="tabBackground">@color/sky_color_white</item>
        <item name="tabTextAppearance">@style/CustomAccountTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/sky_color_00a5ea</item>
    </style>
    <style name="CustomAccountTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/sky_font_size_15</item>
        <item name="android:textColor">@color/sky_color_808080</item>
    </style>

    <!-- 订单TAB样式 -->
    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/sky_color_00a5ea</item>
        <item name="tabIndicatorHeight">@dimen/sky_dp_size_2</item>
        <item name="tabBackground">@color/sky_color_white</item>
        <item name="tabTextAppearance">@style/CustomTabTextAppearance</item>
        <item name="tabSelectedTextColor">@color/sky_color_00a5ea</item>
    </style>
    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/sky_font_size_18</item>
        <item name="android:textColor">@color/sky_color_808080</item>
    </style>

    <!-- 商品分类 -->
    <style name="product_category_dialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
    <style name="product_category_animation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/app_guide_in</item>
        <item name="android:windowExitAnimation">@anim/app_guide_out</item>
    </style>
</resources>